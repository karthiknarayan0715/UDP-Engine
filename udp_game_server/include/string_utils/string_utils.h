#include <iostream>
#include <vector>

std::vector<std::string> split(std::string str, std::string delimiter);

// std::string charPtrToStr(char* charPtr, int size);

// char* StripBuffer(char* buffer, int startIndex, int endIndex);

// char* StripBuffer(char* buffer, int startIndex);

// void StringCopy (char* target, char* source);